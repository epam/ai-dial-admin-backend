# Upgrade Plan — 0.17.0

> Auto-generated from `docs/INFRA-CHANGELOG.md` — review before applying.

## 1. High-Priority Changes

### 1.1 Security & RBAC

| Setting | Default | Required (Yes/No) | Description |
|---|---|---|---|
| `config.rest.security.default.allowedRoles` | - | No | **Removed.** No longer supported. Use `config.rest.security.default.roles-mapping` instead. |
| `providers.*.allowed-roles` | - | No | **Removed.** No longer supported. Use `providers.*.roles-mapping` instead. |

**Helm values.yaml:**
```yaml
env:
  # Remove these:
  # config.rest.security.default.allowedRoles
  # providers.azure.allowed-roles
```

---

## 2. Medium-Priority Changes (defaults changed — review)

### 2.1 Security & RBAC

| Setting | Default | Required (Yes/No) | Description |
|---|---|---|---|
| `config.rest.security.default.roles-mapping` | `{"ConfigAdmin":["FULL_ADMIN"],"admin":["FULL_ADMIN"]}` | No | Default changed from `{}`. Ensure this default mapping aligns with your identity provider configuration. |

---

## 3. Low-Priority / Optional Changes

### 3.1 Configuration & API

| Setting | Default | Required (Yes/No) | Description |
|---|---|---|---|
| `CORE_CONVERSATIONS_METADATA_DEFAULT_LIMIT` | `256` | No | Default limit on the number of items in the conversations metadata response from DIAL Core. |
| `CONVERSATIONS_IMPORT_CONSECUTIVE_ERRORS_THRESHOLD` | `2` | No | Maximum number of consecutive errors allowed during conversations import. |

### 3.2 Observability

| Setting | Default | Required (Yes/No) | Description |
|---|---|---|---|
| `METRICS_INFLUX2_DEFAULT_PAGE_SIZE` | - | No | **Removed.** No longer supported. |
| `METRICS_INFLUX3_DEFAULT_PAGE_SIZE` | - | No | **Removed.** No longer supported. |

---

## Pre-flight Checklist

- [ ] Removed deprecated `allowedRoles` properties from configuration
- [ ] Reviewed **Medium** default change for `roles-mapping`
- [ ] Helm values.yaml / docker-compose updated
- [ ] RBAC / security config reviewed
- [ ] Smoke test passed after upgrade
